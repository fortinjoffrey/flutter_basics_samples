name: fighting_zombie_with_flame
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flame: ^1.8.1
  flutter:
    sdk: flutter

dev_dependencies:
  assets_gen: ^1.5.0
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/

assets_gen:
