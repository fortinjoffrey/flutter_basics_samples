import 'package:flutter/material.dart';

abstract class Constants {
  // COLORS
  static const mainColor = Color(0xFFF65B0C);
  static const secondaryColor = Color(0xFF7F8B8C);
  static final lightMainColor = mainColor.withAlpha(80);

  // SIZES
  static const onboardingMaxWidth = 450.0;
}
