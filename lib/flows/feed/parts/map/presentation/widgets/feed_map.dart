part of '../../../../presentation/screens/feed_screen.dart';

class _FeedMap extends StatelessWidget {
  const _FeedMap();

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: const Center(
        child: Text('Map'),
      ),
    );
  }
}
